services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
    command: npm start

  validator:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    command: npm run validate
